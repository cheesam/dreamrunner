<div class="container">
  <div class="row">
    <div class="col-sm-6 offset-sm-3">
      <%= render 'form', review: @review, dream: @dream %>
      <%= link_to "Delete review", review_path(@review),
          method: :delete,
          data: { confirm: "Are you sure?" }, class: "btn btn-ghost mt-3 ml-3" %>
    </div>
  </div>
</div>
